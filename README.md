# Docker
`docker-compose build --no-cache
docker-compose up`

- set `OPEN_AI_KEY` IN `.env`
``OPENAI_API_KEY=your_openai_api_key
``
- set configurations of support languages, directory (input  of scanning) and output results in config.yam
 
